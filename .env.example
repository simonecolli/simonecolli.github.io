# Optional local override: copy to .env.local.
# Production is enabled in .env.production after the owner’s account checks.
# Set false to disable the banner and all collection.
VITE_ANALYTICS_ENABLED=false
